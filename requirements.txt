# MAX AI Bot — runtime requirements
# (versions are floors; the build pins what's resolved at build time)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2
aiosqlite>=0.19
httpx>=0.27
maxapi>=0.13.0
duckduckgo_search>=4.1
trafilatura>=1.6
scrapling>=0.2.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23

"""CLI entry points for the bot (F2, 2026-08-21).

These scripts are NOT part of the runtime — they exist for smoke-
testing and operator workflows (e.g. running a research pipeline
locally without the MAX frontend). Invoked as ``python -m
app.cli.<name>`` so imports resolve to the project package.
"""
